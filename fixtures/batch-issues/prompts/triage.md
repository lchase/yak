Read every file under `fixtures/batch-issues/src/` and its matching test
under `fixtures/batch-issues/test/`. There are three small, independent
bugs — one per file.

Write a short summary of each bug (what's wrong, not how to fix it — the
fix happens separately, per file) to `fixtures/batch-issues/BUGS.md`, one
section per file. This file is shared context for whoever fixes each bug
next.
