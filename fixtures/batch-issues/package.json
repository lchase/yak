{
  "name": "batch-issues-fixture",
  "private": true,
  "version": "0.0.1",
  "description": "Three small, independent bugs, for yak's map fan-out cookbook pattern",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}
