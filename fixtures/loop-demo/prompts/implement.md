You're implementing `mergeIntervals` in `fixtures/loop-demo/src/mergeIntervals.ts`.

Read `fixtures/loop-demo/src/mergeIntervals.ts` (the spec is in its docstring)
and `fixtures/loop-demo/test/mergeIntervals.test.ts` (the test cases it must
satisfy). Implement the function so every test passes.

Feedback from the previous round's reviewer: {{priorFeedback}}

Make your edit now, then summarize what you changed.
