You're implementing `titleCase` in `fixtures/release-demo/src/titleCase.ts`.

Read `fixtures/release-demo/src/titleCase.ts` (the spec is in its docstring)
and `fixtures/release-demo/test/titleCase.test.ts` (the test cases it must
satisfy). Implement the function so every test passes.

Make your edit now, then summarize what you changed.
