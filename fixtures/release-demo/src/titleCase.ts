/**
 * Convert a string to title case: capitalize the first letter of every
 * word, lowercase the rest. Words are separated by whitespace. Small
 * connector words (a, an, the, of, in, on, and, or, but, to) stay
 * lowercase unless they're the first word.
 */
export function titleCase(input: string): string {
  throw new Error('not implemented')
}
